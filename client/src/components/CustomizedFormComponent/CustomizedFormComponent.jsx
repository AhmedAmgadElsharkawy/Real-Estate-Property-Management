import styles from "./CustomizedFormComponent.module.css"

function CustomizedFormComponent({onSubmit}) {
  return (
    <form onSubmit={onSubmit}>
        
    </form>
  )
}

export default CustomizedFormComponent